name: Push

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    timeout-minutes: 5
    # environment: production
    steps:
      - uses: actions/checkout@v5.0.0
      - name: test
        run: |
          echo before
          echo $FOO
          echo $FOO | tr ' ' '-'
          echo after
          echo 'This is secret'
          echo $BAR
          echo $BAR | tr ' ' '-'
          echo $GITHUB_SHA
          echo done
        env:
          FOO: ${{ secrets.FOO }}
          BAR: ${{ secrets.BAR }}
